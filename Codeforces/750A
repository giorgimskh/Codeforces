#include <iostream>
using namespace std;

int main(){
    short n,k;
    cin >> n >> k;
    n+=1;
    
    while(n--){
        short sum=(1+n)*n/2*5;
        if(sum+k<=240) {cout << n; break;}
    }
}
