#include <iostream>
using namespace std;

int main(){
    short t,a,b,c;
    cin >> t;
    
    while(t--){
        cin >> a >> b >> c;
        //create increasing order
        if(a>b) swap(a,b);
        if(b>c) swap(b,c);
        if(a>b) swap(a,b);      
        cout << b << endl;
    }
}
