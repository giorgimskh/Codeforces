#include <iostream>
using namespace std;

int main (){ 
    short t,a,b,c,d;
    cin >> t;
    
    while(t--){
        short ans=0;
        cin >> a >> b >> c >> d;
        
        if(b>a) ++ans;
        if(c>a) ++ans;
        if(d>a) ++ans;
        
        cout <<ans <<endl;
    }
}
