#include <iostream>
using namespace std;

int main(){
    short test,x=0;
    string s;
    cin >> test;
    
    while(test--){
        cin >> s;
        if(s[0]=='+' || s[2]=='+') ++x;
            else --x;
    }
    cout << x;
}
