#include <iostream>
using namespace std;

int main(){
    short k,n,ans=0;
    cin >> n >> k;
    short a[n];
    
    for(int i=0;i<n;++i) cin >> a[i];
    for(int i=0;i<n;++i) {
        if(a[i]>=a[k-1] && a[i]>0) ++ans;
            else break;
    }
    cout << ans;   
}
