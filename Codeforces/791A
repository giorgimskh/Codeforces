#include <iostream>
using namespace std;

int main(){
    short a,b;
    cin >> a >> b;
    
    if(a>b) cout << 0;
        else {
            int ans=0;
            
            while(a<=b){
                a*=3;
                b*=2;
                ++ans;
        }
        cout << ans;
    }
}
