#include <iostream>
using namespace std;

int main(){
    short n,m;
    cin >> n >> m;
    
    if(m%2==0 && n > m) cout << "Malvika";    
    else if(m%2!=0 && n > m) cout << "Akshat";
    else   if(n%2==0 && n <= m ) cout << "Malvika";
        else cout << "Akshat";        
}
