#include <iostream>
using namespace std;

int main(){
    int n,ans=0;
    string s,s1="0";
    cin >> n;
    
    while(n--){
        cin >> s;
        if(s!=s1){
            s1=s;
            ans++;
        }
    }
    cout << ans;
}    
