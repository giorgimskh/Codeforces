#include <iostream>
using namespace std;

int main(){
    short test,n,sum,k;
    cin >> test;
    
    while(cin >> n){
        sum=0;
        
        while(n--) {
            cin >> k;
            sum+=k;
        }
        cout << (sum%2 ? "NO":"YES") << endl;
    }
}
