#include <iostream>
using namespace std;

int main(){
    int n,check;
    short k,w;
    cin >> k >> n >> w;
    
    check=(k+w*k)*w/2; // sum of arrithmetic progression
    cout << (check>n ? check-n:0);
}
